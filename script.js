function lekcijaDva() {
    var password = prompt("Unesite lozinku da biste preuzeli fajl:");
    if (password === "excelguru") {
        window.open("Lekcija 2.zip")
    } else if (password === null) {
        return;
    } else {
        alert("Neispravna lozinka, probajte ponovo!");
    }
}


function lekcijaTri() {
    var password = prompt("Unesite lozinku da biste preuzeli fajl:");
    if (password === "excelninja") {
        window.open("Lekcija 3.zip")
    } else if (password === null) {
        return;
    } else {
        alert("Neispravna lozinka, probajte ponovo!");
    }
}

function lekcijaCetiri() {
    var password = prompt("Unesite lozinku da biste preuzeli fajl:");
    if (password === "excelexpert") {
        window.open("Lekcija 4.zip")
    } else if (password === null) {
        return;
    } else {
        alert("Neispravna lozinka, probajte ponovo!");
    }
}

function lekcijaPet() {
    var password = prompt("Unesite lozinku da biste preuzeli fajl:");
    if (password === "sundjerbob") {
        window.open("Lekcija 5.zip")
    } else if (password === null) {
        return;
    } else {
        alert("Neispravna lozinka, probajte ponovo!");
    }
}